[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmds = [
  "cp nginx.conf /etc/nginx/conf.d/default.conf",
  "nginx -g 'daemon off;'"
]

[static]
buildDir = "dist"

